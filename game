  function computerPlay(){
    var choice=Math.floor(Math.random() * 3)  //values 0 to 2
    if (choice == 0) 
    return ("Rock");
    else if (choice == 1)
    return ("Paper");
    else if (choice == 2)
    return ("Scissors");
  }
